type User {
	id: String!,
	firstName: String!,
	lastName: String!
}

input RegisterArgs {
	firstName: String!,
	lastName: String!
}

type Mutation {
	register(user: RegisterArgs!): User!
}

type Query {
	getUsers() : [User]!
}
