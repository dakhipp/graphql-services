enum Roles {
  ADMIN
  OWNER
  USER
}

type User {
	id: String!,
	firstName: String!,
	lastName: String!
}

type Session {
  id: String!,
  roles: [Roles!]!
}

input RegisterArgs {
	firstName: String!,
	lastName: String!,
  email: String!,
  phone: String!,
  password: String!,
  passwordConf: String!
}

input LoginArgs {
  email: String!,
  password: String!,
}

type Mutation {
	register(args: RegisterArgs!): Session
  login(args: LoginArgs!): Session
}

type Query {
	getUsers() : [User!]!
}
